---

# The following variables can be modified for your personal usage
azure_location: 'East US'

azure_resource_group: "{{ lookup('env','AZURE_RESOURCE_GROUP') }}"

rhpds_ssh_private_key_path: '~/.ssh/id_rsa_rhpds'

rhpds_ssh_public_key_path: "{{ rhpds_ssh_private_key_path + '.pub' }}"

# Do  not modify the following variable
tf_project_dir: terraform
